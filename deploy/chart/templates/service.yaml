apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernia.fullname" . }}
  labels:
    {{- include "kubernia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernia.selectorLabels" . | nindent 4 }}
