# Umsetzungs-Reihenfolge (alle Tickets)

> **#507 erledigt (2026-07-02, iSAQB A11 — DNS-1123 an allen Namensgrenzen):** Die Namensprüfung ist von der einzelnen `kubectl create`-Call-Site (#489) in die geteilten `_make*`-Fabriken gewandert (`_makeDeployment`/neu `_makeService`/`_makePvc`/`_makeStatefulSet`); `Sim.exec()` fängt den `InvalidResourceNameError` an der Aggregat-Grenze und macht daraus die richtige kubectl-Meldung. Damit greift die Regel an ALLEN Anlege-Wegen — helm install (Release→depName aus Roh-Input), `kubectl apply -f` (bisher komplett ungeprüft), expose, StatefulSet/PVC — statt nur bei create. VO in `sim/names.ts` auf `ResourceName` verallgemeinert; der bis dahin tote prüfende Constructor ist jetzt als `resourceName()` live (von den Fabriken zentral genutzt), `podName()` (nie aufgerufen) entfernt. Geteilter Meldungstext `rfc1123ErrorText`/`RFC1123_TIP` (create + Fabrik gleiche Meldung). Neuer Verhaltens-Test `test/sim/resourcename-enforcement.test.ts` (Red-Green am helm-/apply-Pfad verifiziert), 1527 Tests + verify:full + 6 e2e grün. Aus dem Analyse-Block-Kopf (A11) entfernt. **Nächstes freies Ticket = oberstes des Kopfes (#508 A12 — Workload-Aggregat-Mutationen kanalisieren; #538/#500 in Arbeit, Live-Stand prüfen).**
> **#501 erledigt (2026-07-02, iSAQB A8 — Domänen-Ticks szenen-neutral):** Der verifizierte Bug (economyTick lief nur in `WorldScene.update()`, stand in den Regionen still) ist behoben: neues Anwendungs-Bündel `src/game/tick.ts` mit `Game.tick(dtMs)` rückt die frame-unabhängige Domäne (Hafen-Wirtschaft + Spiel-Zeit) an EINER Stelle vor; main.ts treibt es aus Phasers globalem `PRE_STEP` (feuert je Frame unabhängig von der aktiven Szene) → Einkommen UND Kalender ticken jetzt in **jeder** Szene. Die Auszahlung meldet die Anwendung entkoppelt über einen neuen runtime-Sink (`setPayoutSink`/`notifyPayout`, Idiom wie Audio-#344/Save-#497); ui.ts refresht daraus den HUD überall, der `+N 🪙`-Floater kommt nur von der aktiven Hafen-WorldScene (`payoutFloat`, `scene.isActive()`-Guard). Die Zufalls-Gefahren (`tickEvents`) bleiben **bewusst** weltgebunden (spawnen Welt-Sprites = Präsentation) → Entkopplung als Folge-Ticket **#540**. `Game.tick` test-first (Auszahlung+Sink/Uhr/dt-Deckel/NaN-Guard, Red-Green), `verify` grün (1520 Tests), Boot-+Interaktions-Smokes 6/6. **Nächstes freies Ticket = oberstes des Kopfes (#538 A7b — `kubectlApply`→`applyHandlers`; #500/#512 erledigt, Live-Stand prüfen).**
> **#500 erledigt (2026-07-02, iSAQB A9 — Bewertungslogik DOM-frei):** Reine Bewertungslogik aus den DOM-Methoden herausgelöst nach neuem pure-Domäne-Modul `src/viewdecide.ts` (DOM-/Sim-frei, Muster wie overlaykbd.ts): `evaluateSubmission` (Terminal-Wertung: accept-Match / Abkürzungs-Gating #299/#366 / Near-Miss #367 / Begründung #233/#307 / Fehlerzähler, als diskriminiertes Verdikt) + `funkSessionKind` (Session-Priorität) + `scoreReview` (Quiz „mit Hilfe ≠ sicher" #234) + `resolveTalkTarget` (NPC-Routing); radio/quiz/hud setzen die Verdikte nur noch um. Neuer `test/viewdecide.test.ts` (24 Fälle, Red-Green verifiziert), CLAUDE.md-Landkarte ergänzt, 1517 Tests/verify/Coverage/6 e2e grün. Nebenbefund als Folge-Ticket **#539** (Coverage-Domänen-Glob verfehlt Top-Level-Dateien mit reserviertem Präfix — deshalb `viewdecide` statt `uieval`). Aus dem Analyse-Block-Kopf (A9) entfernt.
> **#512 erledigt (2026-07-01):** Reiner Entscheidungskern `src/hazards.ts` (pure Domäne, import-frei) herausgelöst — `resolveHazardTick` (welche Gefahr auflöst/tickt: Sturm behoben, Piraten bei want-Repliken besiegt, Krake bei secrets>baseline vertrieben, Deadline-Misserfolg) + Start-Gate `hazardStartable` + Opfer-Eignung `stormVictims`/`pirateVictims`; `scenes/worldscene/events.ts` führt nur noch die Phaser/UI/Game-Effekte aus. Belohnung/Strafe (Kraken-Fail→Dublonen-Klau) ist damit erstmals unit-testbar (`test/hazards.test.ts`, 20 Fälle, Red-Green am Kraken-baseline-Grenzfall). verify:full grün. Der scenery→decor-Prädikate-Teil bewusst als Folge-Ticket **#537** abgespalten (distinktes Anliegen). Aus dem Analyse-Block-Kopf (A10) entfernt.
> **Stand: 2026-07-02 — #499 (iSAQB A7, Resource-Registry) erledigt:** Die reset/merge/snapshot-Triplizierung der „einfach additiven" Ressourcentypen (services/ingresses/networkPolicies/serviceMonitors/prometheusRules/grafana*) läuft jetzt über EINE `SIMPLE_RESOURCE_KEYS`-Registry in `sim.ts` — je ein Schleifendurchlauf statt drei fast identischer Blöcke (neue solche Ressource = ein Eintrag; iSAQB Open-Closed). Die Eingabe-Parser (`_editDistance`/`_suggest`/`_flagValue`/`_multiFlag`) sind pure Funktionen in `sim/util.ts`, aus fünf Host-Interfaces (Kubectl/Docker/Git/S3/Kubeadm) rausgefallen und brachten `sim.ts` von 799 auf 763 LOC. **Bewusst abgespalten nach #538** (in den MITTEL-Block einsortiert): der `kubectlApply`-Monolith (~300 Zeilen) → `applyHandlers`-Map — eigenständige Umstrukturierung in `kubectl/lifecycle.ts`, zusammen hätte der Slice das Diff-Größenbudget (#533) gesprengt. `verify:full` grün (1493 Tests inkl. util-/Registry-Tests Red-Green, Coverage, beide Builds, 6/6 e2e), snapshot→reset-Roundtrip verlustfrei.
>
> **Stand: 2026-07-01 — Harness & Vorzeige-Doku (#525–#531):** Auf Maintainerin-Wunsch ein zweiter Block **ganz oben** (über dem iSAQB-Block): eine **Vorzeige-README** (#525), die das Spiel _und_ Architektur/Qualität _und_ den KI-Agenten-Harness vermarktet, dazu die kanonische **Harness-Tiefendoku** (#526) und fünf **echte Harness-Verbesserungen**: Aggregat-Kommando `npm run verify` (#527), **Git pre-push-Hook** (#528, schließt die Post-hoc-CI-Lücke des Direkt-Push-auf-main), Harness-Drift-Wächter für Kommandos/Doku-Links (#529), **ADR 0008** KI-Agenten-Harness (#530), Forum-Inbox-Härtung gegen Prompt-Injection (#531). Dazu aus einer **parallelen KI-Fabrik-Analyse** zwei Tooling-Tickets als Low-Prio-Schwanz des Blocks: Multi-Viewpoint-Review-Skill mit Gate-Short-Circuit (#532) und Diff-Größenbudget-Gate (#533) — beide bauen auf dem Gate-/Tooling-Fundament (#527/#529) auf. **#525 (Vorzeige-README) erledigt (2026-07-01): drei getrennte Ebenen — Spiel / Architektur & Qualität / Gebaut von KI-Agenten (Harness); verlinkt arc42 + iSAQB + ADRs, Quest-Zahl-Test grün. #526 (kanonische Harness-Tiefendoku `docs/agent-harness.md`) erledigt (2026-07-01): erklärt den Harness als System (5 Bausteine, jede Fitness-Function mit WAS/WARUM/Red-Green, Autonomie-Schleife + Grenzen, Roadmap), README-Platzhalter auf echten Link umgestellt, arc42 §9 verlinkt sie als vorliegend. #527 (Aggregat-Kommando `npm run verify`) erledigt (2026-07-01): eine SSOT-Kette über alle Gates (typecheck → lint → check:arch → check:size → check:docmap → test), `verify:full` ergänzt Builds + Boot-Smoke; die CI ruft `npm run verify` statt sechs duplizierter Steps, Fitness-Function `test/verify-script.test.ts` sichert die Kette gegen vergessene Gates. #528 (Git pre-push-Hook) erledigt (2026-07-01): versionierter `.githooks/pre-push` (nativ über `core.hooksPath`, kein husky) fährt bei Push auf `main` die schnellen Gates (`npm run verify`, ohne Builds/Smoke) und bricht bei Rot ab; verdrahtet via `npm run setup`, LF über `.gitattributes` erzwungen, Fitness-Function `test/prepush-hook.test.ts`, Umgehung `git push --no-verify`. Der Push selbst lief bereits durch den Hook (dogfood). #529 (Harness-Drift-Wächter `npm run check:docdrift`) erledigt (2026-07-01): scripts/check-docdrift.mjs + test/docdrift.test.ts prüfen vier Fehlklassen jenseits der Datei-Landkarte (tote `npm run`-Kommandos, undokumentierte Kern-Skripte, tote interne Links, tote `#anker` per GitHub-Slug), Code-Fences ausgeblendet; in `verify` + CI eingehängt, docs/agent-harness.md um einen Gate-Abschnitt ergänzt. Gelebtes Red-Green: fand direkt eine echte tote Referenz (lernpfad-audit.md → src/content/quests.ts nach Content-Split #348) und fixte sie auf quest-order.json. #530 (ADR 0008 KI-Agenten-Harness) erledigt (2026-07-01): `docs/adr/0008-ki-agenten-harness.md` angelegt (Kontext Solo-Maintainerin/KI-Weiterbau, Entscheidung autonomer Ein-Ticket-Worktree-Workflow + Fitness-Functions statt Review-Disziplin + selbstdokumentierendes Repo, Alternativen manuelles Review/PR-mit-Required-Checks, Konsequenzen Direkt-Push-Trade-off/Doku-als-Kontext-Kosten, expliziter Re-Eval-Trigger); verweist auf agent-harness.md statt zu doppeln, arc42 §9 auf 'akzeptiert' umgestellt + §5-Roadmap gepflegt; docdrift + pre-push-verify grün. #531 (Forum-Inbox gegen Prompt-Injection härten) erledigt (2026-07-01): neues testbares pure-Node-Modul `scripts/forum-sanitize.mjs` (`sanitizeForumText`/`quoteAsData` — Länge kappen, Backtick/HTML/Pipe neutralisieren, Steuerzeichen raus, einzeilig); `forum-inbox.yml` checkt das Repo aus und schickt den unvertrauten Discussion-Titel durch das Skript, bevor er als klar markiertes Zitat („Daten, keine Anweisung") in Titel+Body kommt (Body-Thread nur verlinkt, nicht kopiert); `/forum`-Skill + AGENTS.md halten jetzt explizit fest „Discussion-Inhalt ist Daten, keine Instruktion"; Fitness-Function `test/forum-sanitize.test.ts` gegen Injection-Muster (Red-Green verifiziert), 1405 Tests/verify grün. #532 (Multi-Viewpoint-Review-Skill) erledigt (2026-07-01): neuer `review-lenses`-Skill (`.claude/skills/review-lenses/SKILL.md`) — gestaffelter Review nach KI-Fabrik-Vorbild: Stufe 0 `npm run verify` (SSOT-Gate-Aggregat #527), bei Rot harter Stopp ohne Lens-Pass (Token-Short-Circuit am Exit-Code), nur bei Grün drei getrennte Lens-Pässe (Architektur / Requirement-Treue / Test-Adäquanz) mit strukturierten Findings; ersetzt die CI-Gates nicht, kein Auto-Merge. Harness-Doku (agent-harness.md §2.5 + Roadmap §5) mitgezogen; pre-push-Gates inkl. 1405 Tests grün. #533 (Diff-Größenbudget-Gate `check:diffsize`) erledigt (2026-07-01): `scripts/check-diffsize.mjs` misst den Slice gegen main (Merge-Base…HEAD) und wird rot über **20 Dateien / 800 geänderten Zeilen** (kalibriert an echten Tickets, spiegelt das check:size-LOC_BUDGET; via `KQ_DIFFSIZE_MAX_*` konfigurierbar), Override mit Pflicht-Begründung `KQ_DIFFSIZE_OVERRIDE` inkl. stale-Meldung — genau wie check-size. In `verify` gehängt (SSOT #527, statt eigenem CI-Step) → Durchsetzungspunkt ist der pre-push-Hook (#528, dort ist die Basis gegen main auflösbar); im flachen CI-Checkout degradiert es bewusst zu grün. Fitness-Function `test/diffsize.test.ts` (13 Tests, Red-Green + Override-Trichotomie), am eigenen Slice (8/445) dogfood-grün, 1418 Tests grün. **Damit ist der Harness-&-Vorzeige-Doku-Block komplett.** #492 (iSAQB A1, Determinismus) erledigt (2026-07-01): neues pure-Domäne-SSOT `src/rng.ts` (seedbarer `mulberry32`/`nextRandom`/`seedGlobalRng` + aus Namen abgeleitete stabile `hashStr`/`hashHex`); `Math.random` aus `src/sim/**`+`src/content/**` verbannt — clusterIP (expose/apply/argocd/helm) und Pod-IP (describe) jetzt deterministisch aus dem Namen (Service behält seine IP, describe konsistent), Docker-Image-ID/-Größe deterministisch (`docker images` wieder reiner Lesebefehl), `randSuffix` + `content/util.ts` `pick`/`rnd` aus dem seedbaren Strom; observability nutzt den gemeinsamen `hashStr`. Doppelt gewächtert: ESLint `no-restricted-properties` (scoped) **+** Fitness-Function `test/rng.test.ts`, beide Red-Green verifiziert; `freshSim()` seedet pro Testfall. 1427 Tests/verify/Builds/6 e2e grün. #493 (iSAQB A2, importData) erledigt (2026-07-01): neuer store-Helfer `SaveStore.migrateParsed` (Format-Logik aus `readState` gespiegelt) hebt den geparsten Stand aufs aktuelle Format; `Game.importData` läuft jetzt durch `migrateParsed` → `sanitizeState` → `writeState` (aktuelle v5-Hülle) statt hüllenlosem `write(json)` — Import umgeht Migration + Sanitize nicht mehr. Import-Pfad in `test/savemigration.test.ts` mit v1..v5-Fixtures + hüllenlosem v0-Stand + kaputtem Fixture abgedeckt (3 Tests RED-GREEN gegen das alte Verhalten verifiziert), 1431 Tests/verify grün. #494 (iSAQB A3, scenario/applyEffects ungeprüft) erledigt (2026-07-01): neues Leaf-Modul `src/content/scenario.ts` mit `reviveScenario(v, path)` — prüft ein Inline-`scenario` strukturell gegen eine geschlossene Feld-Allowlist (`SCENARIO_FIELDS`, spiegelt `interface Scenario`) + eine applyEffect-Schlüssel-Allowlist (`APPLY_EFFECT_KEYS`, spiegelt `interface ApplyEffect`), fail-fast im Check-DSL-Stil. Beide früheren `as Scenario`-Casts (Inline-Schritt in loader.ts + tf-config `sc as unknown as Scenario`) laufen jetzt durch `reviveScenario`; ein Tippfehler im Schlüssel (`deploymnets`, `aplyEffects`) scheitert hart beim Laden statt still im Sim-Verhalten. Bewusst nur strukturell (JSON-Art je Feld), Ressourcen-Semantik füllt/prüft weiter reset()/die Sim. Als eigenes Modul ausgelagert, weil loader.ts sonst das 800-LOC-Budget riss (vgl. #517). Fitness-Function `test/scenario-validate.test.ts` (11 Fälle, Red-Green verifiziert), 1442 Tests + verify:full + 6 e2e grün. #495 (iSAQB A4 — Coverage messen + Per-Verzeichnis-Gate) erledigt (2026-07-01): `@vitest/coverage-v8` + `npm run test:coverage`; Schwellen **pro Schicht** (`vite.config.ts` `LAYER_FLOORS`) aus der Schicht-SSOT `scripts/layers.cjs` (`COVERAGE_GLOBS`, genau EIN Glob je Bucket, Domäne als Extglob-Ausschluss `src/!(scenes|ui|game|…)/**`), **KEINE** globale Schwelle (diese Vitest-Version rechnet Glob-Treffer nicht aus dem Global heraus → sonst bliebe global der Repo-Schnitt). Floors ehrlich leicht unter Ist (Domäne hart 89/80/92/94, Anwendung 79/76/80/80, Präsentation/Einstieg bewusst niedrig aber explizit 3-4 % → kein stilles Absacken auf 0), `autoUpdate` aus = Ratchet nur per Commit nach oben. Eigener CI-Schritt (nicht in der schnellen `verify`/pre-push-Kette wegen ~+6 s Instrumentierung), lokal Teil von `verify:full`. Fitness-Function `test/coverage-config.test.ts` bindet die Glob-Form an die RegExp-Wahrheit (`layerOf`) über JEDE src-.ts-Datei (genau ein Bucket, deckungsgleich) + prüft Werkzeug/CI-Verdrahtung + Floor-Staffelung; Red-Green verifiziert (Domäne-Floor > Ist → Domäne-Glob rot; `sfx` aus Präsentations-Glob → Bindungs-Test rot). 1452 Tests + verify + beide Builds grün. #496 (iSAQB A5 — `WorldSceneLike = any`) erledigt (2026-07-01): `WorldSceneLike` ist nicht mehr `Phaser.Scene & [key:string]:any`, sondern `Phaser.Scene & WorldSceneFields` — ein reines Typ-Interface in `scenes/worldscene/types.ts`, das WorldScene per `implements WorldSceneFields` erfüllt (Compiler garantiert real existierende Felder/Render-Primitive). Alle 6 Systemmodule (clustersync/events/warps/terrain/scenery/mapterrain) sind damit voll typgeprüft statt `any`; die Feld-Typen (DecoItem…Hazards) zogen mit nach `types.ts`, die `DynTagLike`-Doppelpflege in clustersync.ts entfiel (importiert `DynTagData`). Die neue Prüfung deckte prompt zwei echte, von `any` maskierte strict-null-Lücken in `events.ts` auf (`hazards.storm/pirate` in der `.find()`-Closure) → verhaltensgleich gefixt. Kein Import-Zyklus (check:arch grün), 1452 Tests + verify + 6 e2e-Smokes grün. #497 (iSAQB A6 — fehlgeschlagener `save()` unsichtbar) erledigt (2026-07-01): neuer Laufzeit-Sink in `runtime.ts` (`setSaveFailedSink`/`notifySaveFailed`, analog zum Audio-Sink #344) bricht die Schichtgrenze sauber — `Game.save()` wertet jetzt den bool-Rückgabewert von `writeState` aus und hebt einen Fehlschlag (voller localStorage im Fallback-Modus, QuotaExceeded) entkoppelt an die Präsentation; `ui.ts` zeigt daraus einen `hint()`-Toast „Speichern fehlgeschlagen — Fortschritt exportieren". Gemeldet wird **einmal pro Fehler-Episode** (der 5-s-Auto-Save spammt nicht), nach einem wieder geglückten Save re-armt die Meldung. Fitness-Function `test/save-fail.test.ts` (Fehlschlag meldet EINMAL / kein Aufruf bei Erfolg / Re-Arm nach Erfolg; Red-Green verifiziert), 1455 Tests + verify + Boot-/Interaktions-Smokes grün. **Nächstes freies Ticket = #501 (iSAQB A8 — Domänen-Ticks aus Szenen-`update()` → szenen-neutraler Taktgeber; #496/#499 in Arbeit, Live-Stand prüfen).**
> **Nachtrag zwei frische Tickets (2026-07-01):** Auf Maintainerin-Wunsch einpriorisiert (beide `prio:mittel`, im MITTEL-Tier am Ende des iSAQB-Blocks): **#536** (flache `src/`-Ebene nach Subdomänen bündeln — bewusst **vor** den NIEDRIG-Datei-Splits #515/#517, damit die Splits gleich am richtigen Ort landen) und **#535** (neuer, eindeutiger Spielname — „KubeQuest" ist bereits vergeben; Branding, je früher desto weniger Rename-Referenzen).
> **Nachtrag Rest-Sim-Familien (2026-07-01):** Der Domänen-Analyse-Helfer hat terraform/s3/kubeadm/glab nachgescannt → ein neues Ticket **#534** (terraform↔kubeadm Node-/Control-Plane-Duplizierung, in den MITTEL-Block einsortiert); die übrigen Zusatzbefunde als Kommentare an bestehende Tickets gehängt statt dupliziert (#508 kubeadm `.length=0`-Workload-Bypass, #492 kubeadm-Token-Determinismus, #518 s3-/terraform-Idiom-Duplizierung). Alle 5 Analyse-Agenten inkl. Helfer sind sauber beendet.
> **Davor — iSAQB-Architektur-Analyse (#492–#524):** Frische, doku-unabhängige Architektur-Analyse des ganzen Codes nach iSAQB (fünf Durchläufe, je eine Schicht) → [architektur-analyse-2026-07-iSAQB.md](architektur-analyse-2026-07-iSAQB.md). **33 neue Tickets #492–#524** angelegt und als Block **ganz oben** im Kopf einsortiert (HOCH→MITTEL→NIEDRIG). Kernbefund: das Fundament (erzwungene Schichtung, Content-as-Data, versionierte Persistenz, Fitness-Functions) trägt — aber (1) **verhaltensbezogene Governance** fehlt als Gate (Determinismus/Coverage/Komplexität/Bundle/Fehlerdiagnostik) und (2) an einzelnen Grenzen hört die Disziplin auf (`scenario` ungeprüft #494, `importData` umgeht Migration #493, `WorldSceneLike = any` #496, `Math.random` in der Domäne #492). Zwei latente Bugs verifiziert: `economyTick` läuft nicht in RegionScene (#501), `importData` schreibt hüllenlos (#493). **Nächstes freies Ticket = oberstes des Analyse-Blocks (#492).**
> **Davor #484:** Lernlücke geschlossen — drei Quiz-Lernkarten im Kapitel `storage-ephemeral` (Knut): (1) Scheduler plant nach `requests`, bei `request=0` blinde Platzierung → Eviction-Loop; (2) ehrliche `requests` → `Pending` (`Insufficient ephemeral-storage`) ist das ehrliche Signal, kein Rückschritt; (3) den Loop durchbrechen ehrliche requests, nicht Löschen/Limits/CPU. Content-as-Data (`crabquiz/storage.json`), per `chapter` im SR-Pool nach Knuts Eviction-Quest freigeschaltet — **kein Quest-Szenario nötig** (die bestehende storage-ephemeral-Quest liefert den Hands-on-Teil, die Karten kristallisieren den Denkschritt). 1376 Tests/typecheck/lint grün, im Kralle-Quiz verifiziert. Nächstes freies Ticket = oberstes des Kopfes (#485). Die verwandten Lernlücken **#485/#486/#487** in den Lernpfad-Block oben gezogen (Content vor QoL).
> **Davor #481:** Barrierefreiheit-Audit (audit-first) → [`barrierefreiheit-audit.md`](barrierefreiheit-audit.md). Drei Dimensionen geprüft: **Tastatur** vollständig bedienbar (kein Ticket), **Kontraste** flächendeckend WCAG AA erfüllt (kein Ticket — der frühere „Fehler"-Verdacht rechnete gegen die per Cascade überschriebenen alten `:root`-Werte + verwechselte AAA mit AA), **Farb-Status** wenige echte Lücken (Quiz ✓/✗, Docker-Fässer laufend/gestoppt) → Folge-Ticket **#491** (Auto-Rest). Reine Doku-Änderung.
> **Davor #480:** Präsentations-Regressionsnetz über den Boot-Smoke (#391) hinaus — `e2e/interaction.spec.ts` (+ geteilte Helfer `e2e/support.ts`) treibt drei kritische Flows über Tastatur/DOM gegen den echten Offline-Build (`file://`), ohne Test-Hintertür: Terminal-Eingabe (`help` → Ausgabe), Overlay auf/zu (Logbuch/Album/Menü), Onboarding-Quest annehmen → abschließen. Bewusst schlank (kein NPC-Nähe-Overlay/keine Weltbewegung, um flake-frei zu bleiben); Red-Green über sabotierte Tastenkopplung abgesichert. `npm run smoke` = 6 e2e-Tests grün, typecheck + lint grün.
> **Davor #490:** DDD Forts. #479 — die Dublonen-Wirtschaft läuft jetzt hinter einem Value Object `Coins` (neues pure-Domäne-Modul `src/coins.ts`, branded number → serialisiert unverändert als `number`, Save-Format unberührt #350). Die EINE Regel (nicht-negativ + ganzzahlig) + die komplette Arithmetik (Rundung/Streak-Multiplikator/Affordability) liegen an EINER Stelle; `game/economy.ts`, `game/save.ts`, `game/shared.ts` und der Kraken-Klau in `events.ts` gehen darüber, `GameState.coins` ist jetzt `Coins`. Negativer/gebrochener Stand un-repräsentierbar. Reines Refactoring, Verhalten unverändert. 1376 Tests/typecheck/lint/arch/docmap grün.
> **Kuratierung 2026-07-01 (SIG-AI-Harness-Diskussion + arc42-Analyse, [arc42-architektur.md](arc42-architektur.md)):** Auf Maintainerin-Wunsch stehen **alle heutigen Architektur-/Harness-Tickets als ein Block ganz oben** — dazu #480 (Präsentations-Tests) und #481 (Barrierefreiheit). #477/#482 (Kontexte benennen + Doku↔Code-Wächter), **#478** (Aggregat-Invarianten-Grenze), **#483** (kontext-lokale Regeln pro Sub-Modul), **#479** (Value Objects, erster Schritt), **#488** (getippte Aggregat-Mutationen, Forts. #478), **#489** (Namensvalidierung an der create-Grenze, Forts. #479), **#490** (Value Object Dublonen/Wirtschaft, Forts. #479), **#480** (Präsentations-Interaktions-Smokes), **#481** (Barrierefreiheit-Audit → Folge-Ticket #491) und **#484** (Lernlücke Scheduler-Blindplatzierung/Eviction-Loop) sind erledigt. **Nächstes freies Ticket = oberstes des Kopfes** (#485).
>
> _Frühere Tickets (Kurzfassung — volle Details in git-History + Brain `Projekte/KubeQuest`):_ #475 Test-Harness konsolidiert (`test/support/` + `test/factories/`, „Verhalten über öffentliche API testen") · #443 Phaser 4 evaluiert & bewusst verschoben (Renderer-Bug 4.2.0 bei kleine-Welt-Szenen, bleiben auf 3.90.x, Re-Eval per Folge-Ticket) · #296 Reset-Fix im Browser verifiziert (+ #473 angelegt) · #382 Worktree-Pfad-Konvention vereinheitlicht · #441 Knut-Sprite Asset-Hygiene · #362 freies Funken „Was ist gerade passiert?"-Erklärung · #359 `help` im CLI-Format · #358 `help` zeigt nur Freigeschaltetes · #328 Sandbox-Vertiefungs-Quiz · #278 Sammelalbum/Glossar · #279 Lernkarten-Backfill · #460–#466 Aufbau-Bogen (Epic #239, kubeadm + Terraform-Cluster) · #430 Gating-Konsistenz · #281/#282 Keycloak-/CI-Vertiefung · #237 Kralle-Gag.
> Sie ist die **kuratierte Vorne-Auswahl** über die generische Board-Sortierung (Prio→Nummer aus [AGENTS.md](../AGENTS.md)): das oberste freie Ticket des **Kopfes** ist „dran"; was nicht im Kopf steht, fällt automatisch auf Prio→Nummer zurück.

## Wie diese Liste funktioniert — drei Schichten

1. **Kopf (kuratiert, ~15–20 Tickets).** Die nächsten Tickets in **bewusster** Reihenfolge — abhängigkeitsbewusst und prio-informiert, nicht nur nach Label. **„Nächstes Ticket" = das oberste freie hier.** Siehe Tabelle unten.
2. **Auto-Rest.** Alles, was **nicht** im Kopf steht, wird **nicht** handsortiert, sondern fällt auf die generische, deterministische Board-Sortierung zurück: **Prio (`hoch`→`mittel`→`niedrig`→ohne), innerhalb der Prio niedrigste Nummer** (Befehl unten). So bleibt die Pflege billig und die Liste skaliert Stardew-fest, auch wenn der Backlog wächst.
3. **Reaktivierungs-Pool.** Die `status:zurückgestellt`-Tickets — geparkt, nicht verworfen. Werden **progressiv** wieder hereingeholt, wenn der Kopf sich leert (siehe unten). Bis dahin bei der Auswahl übersprungen.

## Vor JEDEM Ticket — bewusst zweifeln (über allem)

Bevor irgendein Ticket angefasst wird, **zuerst zweifeln** — das steht über der Reihenfolge:

1. **Stardew-Scope-Frage:** „Ist das, was ich hier mache, noch sinnvoll, wenn KubeQuest **so groß wie Stardew Valley** wird?" Nur umsetzen, wenn die Antwort Ja ist. Eine Lösung, die heute reicht, bei 10× Inhalt aber dasselbe Problem reproduziert, ist keine Lösung (oberste Regel, [AGENTS.md](../AGENTS.md)).
2. **Bisherige Entscheidungen aktiv anzweifeln** — nicht nur die eigene neue Arbeit. Auch **abgeschlossene Tickets, ADRs und „gesetzte" Annahmen** dürfen falsch sein. Wenn beim Bearbeiten auffällt, dass eine frühere Weiche bei Stardew-Scope nicht trägt: hinterfragen, nicht stillschweigend fortschreiben.
3. **Auffälliges → sofort Ticket anlegen** (Bug, Lücke, Tech-Debt, falsche Annahme) — nicht inline mitfixen, nicht „im Kopf" behalten. Lieber ein Ticket zu viel.
4. **Diese Liste danach pflegen** — neues/aufgefallenes Ticket an die passende Stelle einsortieren (siehe „Pflege" unten). Die Reihenfolge ist ein lebendes Dokument, kein einmaliger Plan.

## Grundsatz-Reviews (bewusst offen halten, nicht festlegen)

Diese Tickets sind **keine „bau-X"-Tickets**, sondern Entscheidungen, die man *reviewt und offen hält* — sie färben alle anderen und stehen darum **nicht** im sortierten Kopf:

- **#355** ⚠️ — **Auslieferungsform: Web-App vs. Desktop-Download (wie Stardew).** Bewusst **nicht** auf eine Option festlegen. Ergebnis als **ergebnisoffener ADR** (`docs/adr/0005-auslieferungsform.md`) mit Re-Eval-Trigger. → bei jeder save-/asset-/build-nahen Änderung mitdenken.

## Was „nächstes Ticket" heißt

Sagt die Maintainerin **„nächstes Ticket"** (für kubequest), dann:

1. **Direkt aus dem Kopf unten wählen — KEIN Vorab-Abgleich der ganzen Liste.** Das **oberste noch offene** Ticket des Kopfes nehmen, das
   - **kein** Assignee hat (Kollisionsschutz — siehe [AGENTS.md › Board-Workflow](../AGENTS.md)), und
   - keinen offenen Branch/Worktree hat.

   **Kein Flag verhindert das Loslegen** — die Flags sagen nur, WIE man loslegt (nicht OB): **🎨 Optik** → dranmachen, den Look während der Umsetzung per Rückfrage abstimmen; **📦 Epic** → mit der **Aufteilung** loslegen (session-große Kinder anlegen, Epic auf done schließen); **⚠️ riskant** → mit dem **Evaluieren** loslegen. Reihenfolge-Hinweise wie „Review zuletzt" (#293) oder „Grundsatz offen halten" (#355) sind reine Positionierung, kein Gate.

   Dabei **nur dieses eine Kandidaten-Ticket** kurz gegen den Live-Stand prüfen (`gh issue view <nr>`: offen? kein Assignee? Branch/Worktree-Gegencheck `git worktree list` + `git branch -a`). Ist es schon geschlossen / vergeben, das **nächste** des Kopfes nehmen. Die **ganze Liste wird NICHT vorab gegen GitHub abgeglichen** — Drift wird erst am Ende eingearbeitet (siehe „Pflege"). Das spart bei jeder Auswahl die teure Komplett-Sichtung.
2. **Ist der Kopf leer** (alle erledigt/vergeben), auf den **Auto-Rest** zurückfallen: das oberste freie Ticket nach **Prio→Nummer** (Befehl im nächsten Abschnitt) — und beim Pflege-Schritt den Kopf wieder auffüllen.
3. Das gewählte Ticket mit dem normalen kubequest-Workflow abarbeiten (self-assignen → eigener Worktree → umsetzen → Tests/Typecheck/Lint grün + im Browser verifizieren → nach `main` → Issue schließen). Details: [AGENTS.md](../AGENTS.md).
4. **Erst NACH getaner Arbeit diese Liste pflegen** — der „puh, fertig"-Schritt (siehe ganz unten).

## Reihenfolge — der Kopf

Leitlinie: **Prio zuerst**, innerhalb gleicher Prio nach Abhängigkeit (was etwas anderes ermöglicht, kommt davor), sonst niedrigste Nummer. Content-Arcs (echter Lernpfad-Fortschritt) stehen über QoL-/System-Features. No-dependency-Content-Füller (#218/#219/#228/#229/#236/#237/#239) lebten im Auto-Rest — keine Abhängigkeit, kein Grund für den Kopf; einige davon sind inzwischen erledigt, der verbleibende design-freie Content (#239/#279/#278) ist hier nach Wert kuratiert, weil der Backlog rein `prio:niedrig`/ohne ist.

> **⭐ Kopf-Reihenfolge (2026-07-01):** Der Block **Harness & Vorzeige-Doku (#525–#533)** ist **komplett abgearbeitet**; oben steht jetzt die **iSAQB-Architektur-Analyse (#492–#524/#534)**, _danach_ der bisherige Lernpfad-/QoL-Kopf. Volle Begründung je Ticket: [architektur-analyse-2026-07-iSAQB.md](architektur-analyse-2026-07-iSAQB.md).

| # | Ticket | Prio | Worum's geht | Warum hier / Abhängigkeit |
|---|--------|------|--------------|---------------------------|
| | **━━━ iSAQB-Architektur-Analyse 2026-07-01 — MITTEL (Struktur-, Testbarkeits- & Governance-Präzisierung) ━━━** | | | |
| A7b | **#538** | mittel | Sim: `kubectlApply`-Monolith (~300 Zeilen) → `applyHandlers`-Map | Rest aus #499 (Registry-Kern + Parser erledigt); eigenständige apply-Pfad-Umstrukturierung, bewusst als eigener Slice. |
| A12 | **#508** | mittel | Sim: Workload-Aggregat-Mutationen kanalisieren (addStatefulSet/removeStatefulSet) | `by-construction`-Zusage wird umgangen. |
| A13 | **#509** | mittel | Sim: Invarianten-Lücken (Eindeutigkeit, referenzielle Integrität, STS-Ordinalnamen) | Wächter ist als SSOT verkauft — muss es sein. |
| A13b | **#534** | mittel | Sim: terraform↔kubeadm Node-/Control-Plane-Duplizierung (provisionNode/removeNode + isControlPlane) | Nachtrag Rest-Sim-Familien; Node-Aufbau über 4 Dateien dupliziert, Node-Aggregat-Bypass. |
| A14 | **#510** | mittel | Save-Versioning-SSOT klären: No-op-Migrationskette vs. feldbasierte Sanitize | Trägt nur additive Änderungen. |
| A15 | **#511** | mittel | Save: `sanitizeState` vervollständigen (stats härten, questStep/taskIdx klemmen) | Grenzfälle rutschen durch den Selbstanspruch. |
| A16 | **#513** | mittel | Anwendung: `GameSelf [key:string]:any` → explizites `GameApi`-Interface | Keine Typprüfung an Bündelgrenzen. |
| A17 | **#514** | mittel | Content: `manifests.ts` von Quests ungenutzt / YAML dupliziert → `manifestRef` | Zwei Wahrheiten für dieselben Manifeste. |
| A18 | **#498** | mittel | Content: maschineller Schema-Drift-Wächter JSON ↔ TS-Typen | ~13k Zeilen JSON ohne Struktur-Schutz. |
| A19 | **#502** | mittel | Governance: Komplexitäts-Metriken (ESLint) statt reinem LOC-Deckel | LOC-Deckel sieht God-Functions nicht. |
| A20 | **#503** | mittel | Governance: Bundle-Size-Budget als CI-Gate | Offline-Build inlined alle Assets, wächst unbemerkt. |
| A21 | **#504** | mittel | Querschnitt: zentrale Fehlerbehandlung (`window.onerror` + Fallback-Overlay) | Laufzeitfehler reißt still das Spiel. |
| A22 | **#505** | mittel | Präsentation: Overlay-Register statt 4× hartkodierter ID-Liste | Basis für Fokus-Management (#506). |
| A23 | **#506** | mittel | Barrierefreiheit: Fokus-Management + ARIA für Modals (distinkt zu #491) | Tastatur-/Screenreader-Bedienung löchrig. Nach #505. |
| | **━━━ Frisch einsortiert (2026-07-01, prio:mittel) — auf Maintainerin-Wunsch ━━━** | | | |
| N1 | **#536** | mittel | Architektur: flache `src/`-Ebene (41 Dateien) nach Subdomänen bündeln — Ziel-Struktur + schrittweise Migration | **Vor** den NIEDRIG-Datei-Splits #515/#517: erst die Ziel-Ordner (z.B. `src/world/`, `src/hud/`) festlegen, damit die Splits gleich am richtigen Ort landen statt später nochmal umzuziehen. Kein Big-Bang (1 Subdomäne = 1 Slice, `layers.cjs`/cruiser/docmap mitziehen). |
| N2 | **#535** | mittel | Neuen, eindeutigen Spielnamen finden (`KubeQuest` ist schon vergeben) | Branding, unabhängig — **je früher, desto weniger verstreute Referenzen** beim Rename. Verfügbarkeit belegen (Web/GitHub/npm/Domain), finaler Name mit Maintainerin abstimmen; Save-Keys nicht brechen. |
| | **━━━ NIEDRIG (Duplizierung, God-Files schneiden, Typmodell, Rest-Lücken) ━━━** | | | |
| A24 | **#515** | niedrig | Anwendung: `store.ts` (695) in backend/slots/versioning/persistence schneiden | Vier Verantwortlichkeiten. |
| A25 | **#516** | niedrig | Sim: Host-Seam verschmälern (`extends ClusterState` → schmale Interfaces, ISP) | Leaky Abstraction ermöglicht Umgehungen. |
| A26 | **#517** | niedrig | Content: `loader.ts` (759) generischer Loader-Helfer + Barrel-Split | Fünffaches Loader-Quartett. |
| A27 | **#518** | niedrig | Sim: Duplizierung einsammeln (`makeService`, `resolveDeploymentRef`, Delete-Block) | 4× clusterIP, 13× Delete-Block. |
| A28 | **#519** | niedrig | Content: Typ-Duplikate zusammenführen (QuizCard/CmdCard, entities↔parse) | Bereits divergent. |
| A29 | **#520** | niedrig | Anwendung: GameState-Typmodell (LeitnerEntry/Position, Quest-Fortschritt entkoppeln) | Vierfach redundante Sync-Last. |
| A30 | **#521** | niedrig | Content: `PRACTICE` nach `data/practice.json` (letzter Content-as-Data-Rest) | Content-im-Code. |
| A31 | **#522** | niedrig | Content: Check-DSL Testlücke (Top-Level `includes`-Regel) | Nur Abdeckung; DSL ist vorbildlich. |
| A32 | **#523** | niedrig | Präsentation: `syncCluster` drosseln + dynamische Pod-Slots statt fixe 36 | Letzte Frame-Budget-Lücke bei Cluster-Scale. |
| A33 | **#524** | niedrig | Testarchitektur: FPS-/A11y-Smoke assertieren (FpsMeter vorhanden) | Billig wegen vorhandener Infrastruktur. |
| | **━━━ Bisheriger Kopf (Lernpfad / QoL / Optik) — erst NACH dem Analyse-Block ━━━** | | | |
| | **— Tiefer Lernpfad (Aufbau-Bogen #239 komplett: #460–#466 erledigt; #279 Backfill + #278 Sammelalbum + #328 Sandbox-Lernthema erledigt; #484 Scheduler-Blindplatzierung erledigt) —** | | | |
| 1 | **#485** | niedrig | Lernlücke: initContainer (füllt das emptyDir vor dem Hauptcontainer) | Lernpfad-Content; docktan die storage-ephemeral/emptyDir-Lektion an (Content vor QoL), thematisch direkte Fortsetzung von #484. |
| 2 | **#486** | niedrig | Lernlücke: Image Garbage Collection (kubelet räumt Images, in-use blockt) | Lernpfad-Content; kubelet-/Node-Housekeeping neben Eviction/DiskPressure (Content vor QoL). |
| 3 | **#487** | niedrig | Lernlücke: QoS-Klassen vollständig (BestEffort/Burstable/Guaranteed) + Eviction-Prio | Lernpfad-Content; vertieft genau die requests/limits→Eviction-Reihenfolge aus #484/Knut (Content vor QoL). |
| | **— System / QoL —** | | | |
| 4 | **#318** | niedrig | HUD: Einkommensrate des Hafens/Clusters anzeigen (Dublonen/Stunde) | Auto-Rest hochgezogen (Prio→Nummer); kleines HUD-Feature ohne Abhängigkeit. |
| | **— Anlegende / Epic —** | | | |
| 5 | **#277** | niedrig | Ideen-Ticket: weitere Minispiele überlegen & dafür Tickets anlegen | Anlegend, design-frei; erzeugt Folge-Tickets statt direktem Fix. |
| 6 | **#317** 📦 | niedrig | EPIC: Komfort-Funktionen im Shop kaufen + Shop-Überarbeitung | **Epic → mit der Aufteilung loslegen** (session-große Kinder anlegen, Epic auf done schließen). |
| | **— 🎨 Optik / Grafik (werden GANZ NORMAL automatisch gewählt; das Aussehen stimmt der Agent WÄHREND der Umsetzung per Rückfrage mit der Maintainerin ab — Referenz/Vorschlag/generiertes Asset vorlegen, entscheiden lassen, iterieren; NICHT vorab gaten, NICHT selbst das Design festlegen) —** | | | |
| 7 | **#183** 🎨 | niedrig | Hafen-Kanone als Pixelart-Asset statt Emoji 💣 | Optik — Asset-Look während der Umsetzung abstimmen. |
| 8 | **#186** 🎨 | niedrig | Außen-Türen der Gebäude als Pixelart statt prozeduraler Rechtecke | Optik — Look während der Umsetzung abstimmen. |
| 9 | **#187** 🎨 | niedrig | Interior-Einrichtung (Bullaugen/Türen/Wandschatten) als Pixelart | Optik — Look während der Umsetzung abstimmen. |
| 10 | **#190** 🎨 | niedrig | Overlay-Panels (Funkgerät/Logbuch/Shop/Quiz/Stapel/Menü) im Stardew-Look | Optik — Look während der Umsetzung abstimmen. |
| 11 | **#204** 🎨 | niedrig | HUD-/Panel-Emojis durch PixelLab-Pixel-Icons ersetzen | Optik — Look während der Umsetzung abstimmen. |
| 12 | **#223** 🎨 | niedrig | Rang-Aufstieg mit Feier-Popup (alter → neuer Rang) statt nur Toast | Optik/UX — gehört mit #314 zusammen; Look während der Umsetzung abstimmen. |
| 13 | **#238** 🎨 | niedrig | Container laufen visuell in Pods (Fässer im Schiffsrumpf) | Optik/Visualisierung — Look während der Umsetzung abstimmen. |
| 14 | **#289** 🎨 | niedrig | Kenney-Tilesets (town/dungeon) durch PixelLab ersetzen, dann entfernen | Auto-Rest hochgezogen (Prio→Nummer); Optik — Look während der Umsetzung abstimmen. |
| 15 | **#303** 🎨 | niedrig | Gestoppte Container visuell ins Lager verschieben (statt am Dock) | Auto-Rest hochgezogen (Prio→Nummer); Optik/Visualisierung — Look während der Umsetzung abstimmen. |
| 16 | **#314** 🎨 | niedrig | Zentrales Feier-Popup-System (Konfetti + Spruch) | Optik — Look während der Umsetzung abstimmen (übergreift #223). |
| | **— Zuletzt —** | | | |
| 17 | **#293** | niedrig | Spiellogik-Review (anlegend) | Steht bewusst **zuletzt** (reine Positionierung, kein Gate) — erst wenn der Backlog weitgehend leer ist, sonst veraltet das Review sofort. Erzeugt Folge-Tickets. |

> **#443 (Phaser 4)** ist aus dem Kopf raus: evaluiert und bewusst verschoben (Renderer-Bug in 4.2.0 bei kleine-Welt-Szenen, kein Quick-Fix). Re-Eval läuft über das Folge-Ticket, sobald Phaser 4 reift / der Bug upstream gefixt ist. Details: [ADR 0001](adr/0001-engine-phaser.md).

> **Aufbau-Bogen-Optik #467** 🎨 (zerstörter Hafen → Wiederaufbau) wird wie jedes Optik-Ticket normal gewählt; der Look wird während der Umsetzung mit der Maintainerin abgestimmt — kein Vorab-Gate, kein Blocker für den Lerninhalt (#460–#466).

> 🎨 **Optik-/Grafik-Tickets** (z.B. #183/#186/#187/#190/#204/#223/#238/#289/#303/#318/#341/#342/#467): werden **automatisch wie jedes andere Ticket gewählt**. Das konkrete Aussehen legt der Agent **nicht selbst** fest, sondern stimmt es **während der Umsetzung per Rückfrage** mit der Maintainerin ab (Stardew-Referenz lesen — [AGENTS.md › Grafik-Stil](../AGENTS.md), [docs/stardew-referenz.md](stardew-referenz.md) —, dann Vorschlag/Referenz/generiertes Asset vorlegen und entscheiden lassen, iterieren). Also: dranmachen ja, Design-Entscheidung interaktiv.

## Auto-Rest — alles unterhalb des Kopfes

Steht ein Ticket **nicht** im Kopf, wird es **nicht** hier gepflegt, sondern nach der generischen Regel gewählt: **Prio (`hoch`→`mittel`→`niedrig`→ohne), dann niedrigste Nummer**. Fertig sortierte freie Auswahl in einem Befehl (oberste Zeile ist „dran"):

```bash
gh issue list --state open --limit 500 --json number,title,assignees,labels --jq '
  map(select((.assignees|length)==0))
  | map(select(any(.labels[];.name=="status:zurückgestellt")|not))
  | map(.prio = (if any(.labels[];.name=="prio:hoch") then 0 elif any(.labels[];.name=="prio:mittel") then 1 elif any(.labels[];.name=="prio:niedrig") then 2 else 3 end))
  | sort_by(.prio, .number)
  | .[] | "\(["hoch","mittel","niedrig","ohne"][.prio])\t#\(.number)\t\(.title)"'
```

(Branch/Worktree-Gegencheck trotzdem: `git worktree list` + `git branch -a`.) Diese Tickets müssen **nicht** in den Kopf gehoben werden — der Kopf wächst nur, wenn eine **Abhängigkeit** eine bewusste Vorziehung verlangt.

## Reaktivierungs-Pool — zurückgestellt, nicht verworfen

Die offenen Tickets mit Label **`status:zurückgestellt`** sind **nicht** verworfen. Leert sich der Kopf, werden die passenden **progressiv reaktiviert** (Label entfernen → an dependency-passende Stelle in den Kopf einsortieren). Nicht alles auf einmal — der Kopf wächst kontrolliert.

**Reaktivieren mit Augenmaß:** Manche können bedenkenlos reaktiviert werden. Andere brauchen zuerst eine **Scope-Klärung** (z.B. **#420** NPC-Tagesplan/Routinen, **#421** Item-/Inventar-Modell: was bedeutet „Stardew-Scope" für ein K8s-Lernspiel — Routinen/Crafting, oder vor allem Lern-Tiefe?) — die erst klären, dann reaktivieren.

Bei der Auswahl werden sie **übersprungen**, solange das Label dran ist — **maßgeblich ist immer das Label**, nicht eine Aufzählung hier.

Aktuellen Pool ansehen:

```bash
gh issue list --state open --limit 500 --label "status:zurückgestellt" \
  --json number,title,labels \
  --jq '.[] | "#\(.number)\t\(([.labels[].name]|map(select(startswith("area:")))|join(","))//"-")\t\(.title)"' | sort -n
```

## Pflege dieser Liste — der „puh, fertig"-Schritt

Diese Liste ist **lebendig** — sie wird **am Ende jedes Tickets** fortgeschrieben, **nicht** als Vorab-Check vor der Auswahl. Nach getaner Arbeit einmal den echten GitHub-Stand holen und Drift einarbeiten:

```bash
# Aktive (nicht zurückgestellte) offene Tickets, sortiert Prio→Nummer — Abgleich gegen den Kopf
gh issue list --state open --limit 500 --json number,title,labels,assignees --jq '
  map(select(any(.labels[];.name=="status:zurückgestellt")|not))
  | map(.prio = (if any(.labels[];.name=="prio:hoch") then 0 elif any(.labels[];.name=="prio:mittel") then 1 elif any(.labels[];.name=="prio:niedrig") then 2 else 3 end))
  | sort_by(.prio, .number)
  | .[] | "\(["hoch","mittel","niedrig","ohne"][.prio])\t#\(.number)\tassignee:\([.assignees[].login]|join(","))\t\(.title)"'
```

Dann:

- **Gerade erledigt / sonst geschlossen** → Zeile aus dem Kopf entfernen (GitHub-Issue-Status ist die SSOT für „erledigt"; erledigte Tickets werden hier **entfernt, nicht durchgestrichen**).
- **Kopf zu kurz geworden** (< ~15) → von oben aus dem **Auto-Rest** (Prio→Nummer) **und/oder dem Reaktivierungs-Pool** nachfüllen, bis wieder ~15–20 erreicht sind. Aus dem Pool nur reaktivieren, was wirklich dran ist (Scope-Klärung beachten).
- **Neues offenes Ticket mit echter Abhängigkeit** → an die dependency-passende Stelle in den Kopf einsortieren, nicht unten anhängen. Ein Ticket **ohne** besondere Abhängigkeit muss **nicht** in den Kopf — es lebt im Auto-Rest.
- **Altentscheidung wackelt** → als Grundsatz-Review oben aufnehmen (wie #355), nicht stillschweigend fortschreiben.
- **Driftet die Liste → Doku fixen, Stand-Datum oben aktualisieren, committen** (Doku-only → kein Test-Lauf).
- Bei Unklarheit über die Position: „Ist das okay, wenn KubeQuest Stardew-groß wird?" (oberste Regel, [AGENTS.md](../AGENTS.md)) entscheidet vor dem Prio-Label.
