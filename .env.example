# KubeQuest – lokale Umgebungsvariablen (Vorlage). NUR die Variablen-NAMEN, KEINE Werte.
#
# Zum Aktivieren: diese Datei nach `.env` kopieren und einen Wert eintragen.
# `.env` ist gitignored – der WERT landet nie im (öffentlichen) Repo.

# Passwort für das Dev-/Test-Panel (#325). Nur gesetzt → das Panel ist im
# Dev-Build per Taste F9 (mit dieser Eingabe) erreichbar. Leer/ungesetzt → das
# Panel bleibt gesperrt. Im Prod-/Offline-Build existiert das Panel ohnehin nicht.
#
# Dieselbe Variable wird auch vom verteilbaren Dev-Panel-Build (#331,
# `npm run build:devpanel`) zur Build-Zeit eingebacken. In der GitHub-Actions-CI
# kommt der Wert aus dem gleichnamigen Repo-Secret (Settings → Secrets and
# variables → Actions → VITE_KQ_DEVPANEL_PW) – nicht aus dieser Datei.
#
# Beim Dev-Panel-Docker-Image (#334) wird genau diese Variable stattdessen zur
# LAUFZEIT übergeben (`docker run -e VITE_KQ_DEVPANEL_PW=…`) und beim Containerstart
# in die Seite injiziert – kein Rebuild pro Passwort. Siehe docs/devpanel-docker.md.
VITE_KQ_DEVPANEL_PW=
